## start.sh 脚本功能
使用方法：

脚本特性：

1. 自动检测 Python 解释器 ：优先使用项目内的 .venv 虚拟环境
2. 顺序执行 ：对于组合参数（如 12 ），会先执行完前一个脚本才执行下一个
3. 错误处理 ：如果某个脚本执行失败，会终止后续脚本执行并显示退出码
4. 参数解析 ：支持多个参数，每个参数的每个数字都会按顺序解析执行
测试验证结果：

参数 行为 结果 无参数 显示帮助信息 ✅ 正确 1 启动 1.task_id.py ✅ 正确识别脚本 2 启动 2.model.py ✅ 正确识别脚本 12 先启动 1 ，完成后启动 2 ✅ 正确按顺序执行
 注意 ：脚本执行时出现的浏览器连接错误是因为 DrissionPage 与 Chrome 版本不兼容导致的，与 start.sh 的参数解析逻辑无关。脚本本身的逻辑已验证正确。